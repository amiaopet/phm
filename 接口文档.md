# PHM接口文档

## 认证相关接口

### 登录接口
- **路径**: `/api/login`
- **方法**: `POST`
- **请求体**:
  ```json
  {
    "username": "用户名",
    "password": "密码"
  }
  ```
- **响应**:
  ```json
  {
    "success": true,
    "message": "登录成功",
    "data": {
      "sessionId": "会话ID"
    }
  }
  ```
- **说明**: 用于用户登录系统，成功后返回sessionId用于后续接口认证

## 配置管理接口

### 获取用户配置
- **路径**: `/api/config`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID`
- **响应**:
  ```json
  {
    "success": true,
    "data": {
      "bleedSettings": {
        "cfmThreshold": "38",
        "v2500Threshold": "38",
        "employees": "员工编号",
        "emailRecipients": "邮箱地址",
        "autoRun": {
          "enabled": true,
          "hour": "08",
          "minute": "00",
          "dateRange": "三个月"
        }
      },
      "oxygenSettings": {
        "lastAircraft": "机型",
        "daysRange": 30,
        "psi1Threshold": 1800,
        "psi2Threshold": 1800,
        "employees": "员工编号",
        "emailRecipients": "邮箱地址",
        "autoRun": {
          "enabled": true,
          "hour": "08",
          "minute": "00",
          "dateRange": "三个月"
        }
      }
    }
  }
  ```
- **说明**: 获取用户的配置信息，包括引气监控和氧气监控的相关设置

### 保存用户配置
- **路径**: `/api/config`
- **方法**: `POST`
- **请求头**: `Session-Id: 会话ID`
- **请求体**:
  ```json
  {
    "bleedSettings": {
      "cfmThreshold": "38",
      "v2500Threshold": "38",
      "employees": "员工编号",
      "emailRecipients": "邮箱地址",
      "autoRun": {
        "enabled": true,
        "hour": "08",
        "minute": "00",
        "dateRange": "三个月"
      }
    },
    "oxygenSettings": {
      "lastAircraft": "机型",
      "daysRange": 30,
      "psi1Threshold": 1800,
      "psi2Threshold": 1800,
      "employees": "员工编号",
      "emailRecipients": "邮箱地址",
      "autoRun": {
        "enabled": true,
        "hour": "08",
        "minute": "00",
        "dateRange": "三个月"
      }
    }
  }
  ```
- **响应**:
  ```json
  {
    "success": true,
    "message": "配置保存成功"
  }
  ```
- **说明**: 保存用户的配置信息

## 故障诊断接口

### 启动引气监控
- **路径**: `/api/bleed-monitor`
- **方法**: `POST`
- **请求头**: `Session-Id: 会话ID`
- **请求体**:
  ```json
  {
    "aircraftType": "机型",
    "aircraftNo": "机号",
    "startDate": "开始日期",
    "endDate": "结束日期",
    "thresholdValue": 38,
    "employees": "员工编号",
    "emailRecipients": "邮箱地址"
  }
  ```
- **响应**:
  ```json
  {
    "success": true,
    "data": {
      "taskId": "任务ID",
      "status": "running",
      "message": "任务已启动"
    }
  }
  ```
- **说明**: 启动引气监控分析任务，返回任务ID用于后续查询任务状态和结果

### 启动氧气监控
- **路径**: `/api/oxygen-monitor`
- **方法**: `POST`
- **请求头**: `Session-Id: 会话ID`
- **请求体**:
  ```json
  {
    "aircraft": "机号",
    "startDate": "开始日期",
    "endDate": "结束日期",
    "psi1Threshold": 1800,
    "psi2Threshold": 1800,
    "employees": "员工编号",
    "emailRecipients": "邮箱地址"
  }
  ```
- **响应**:
  ```json
  {
    "success": true,
    "data": {
      "taskId": "任务ID",
      "status": "running",
      "message": "任务已启动"
    }
  }
  ```
- **说明**: 启动氧气监控分析任务，返回任务ID用于后续查询任务状态和结果

### 获取任务状态
- **路径**: `/api/task/{task_id}`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID`
- **响应**:
  ```json
  {
    "success": true,
    "data": {
      "taskId": "任务ID",
      "status": "completed",
      "progress": 100,
      "message": "任务已完成"
    }
  }
  ```
- **说明**: 获取指定任务的当前状态和进度

### 获取任务结果
- **路径**: `/api/task/{task_id}/result`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID`
- **响应**:
  ```json
  {
    "success": true,
    "data": {
      "taskId": "任务ID",
      "status": "completed",
      "result": {
        // 任务特定的结果数据
      }
    }
  }
  ```
- **说明**: 获取指定任务的执行结果

## 飞机信息接口

### 获取飞机详情
- **路径**: `/api/aircraft/detail/{ac_reg}`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID` (可选)
- **响应**:
  ```json
  {
    "success": true,
    "data": {
      "aircraft_type": "机型",
      "msn": "制造序列号",
      "wv": "重量版本",
      "seat_layout": "座位布局",
      "status": "运行状态",
      "first_fly_date": "首飞日期",
      "deliver_date": "交付日期",
      "max_weight_landing": "最大着陆重量",
      "max_weight_takeoff": "最大起飞重量",
      "max_weight_oilless": "最大无油重量",
      "engine_type": "发动机类型",
      "left_engine_sn": "左发序列号",
      "right_engine_sn": "右发序列号",
      "apu_type": "APU类型",
      "apu_sn": "APU序列号",
      "fsn": "飞机序列号"
    }
  }
  ```
- **说明**: 获取指定飞机的详细信息

### 获取飞机近期航班
- **路径**: `/api/aircraft/recent-flights/{ac_reg}`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID` (可选)
- **参数**:
  - `limit`: 返回航班数量，默认10
  - `start_date`: 开始日期 (可选)
  - `end_date`: 结束日期 (可选)
  - `include_all`: 是否包含所有状态的航班，默认false
- **响应**:
  ```json
  {
    "success": true,
    "data": [
      {
        "flight_no": "航班号",
        "departure": "起飞机场",
        "arrival": "到达机场",
        "schedule_date": "计划日期",
        "status": "航班状态",
        "departure_time": "实际起飞时间",
        "arrival_time": "实际到达时间"
      }
    ]
  }
  ```
- **说明**: 获取指定飞机的近期航班列表

### 获取外站故障
- **路径**: `/api/aircraft/outstation-faults/{ac_reg}`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID` (可选)
- **响应**:
  ```json
  {
    "success": true,
    "data": [
      {
        "fault_date": "故障日期",
        "location": "故障地点",
        "ata": "ATA章节",
        "description": "故障描述",
        "action": "处理措施",
        "status": "状态"
      }
    ]
  }
  ```
- **说明**: 获取指定飞机的外站故障列表

### 获取故障保留项
- **路径**: `/api/aircraft/fault-retentions/{ac_reg}`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID` (可选)
- **响应**:
  ```json
  {
    "success": true,
    "data": [
      {
        "fault_id": "故障ID",
        "fault_date": "故障日期",
        "ata": "ATA章节",
        "description": "故障描述",
        "limit_date": "限制日期",
        "status": "状态"
      }
    ]
  }
  ```
- **说明**: 获取指定飞机的故障保留项列表

### 获取缺陷保留项
- **路径**: `/api/aircraft/defect-retentions/{ac_reg}`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID` (可选)
- **响应**:
  ```json
  {
    "success": true,
    "data": [
      {
        "defect_id": "缺陷ID",
        "defect_date": "缺陷日期",
        "ata": "ATA章节",
        "description": "缺陷描述",
        "limit_date": "限制日期",
        "status": "状态"
      }
    ]
  }
  ```
- **说明**: 获取指定飞机的缺陷保留项列表

## 通用接口

### 健康检查
- **路径**: `/api/health`
- **方法**: `GET`
- **响应**:
  ```json
  {
    "status": "healthy",
    "timestamp": "2023-10-15T12:00:00.000Z"
  }
  ```
- **说明**: 用于检查API服务是否正常运行

## 自定义可视化接口

### 获取数据库连接列表
- **路径**: `/api/visualization/db-connections`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID`
- **响应**:
  ```json
  {
    "success": true,
    "data": [
      {
        "id": "conn_id",
        "name": "连接名称",
        "type": "mysql",
        "description": "连接描述"
      }
    ]
  }
  ```
- **说明**: 获取用户已保存的数据库连接列表

### 保存数据库连接
- **路径**: `/api/visualization/db-connections`
- **方法**: `POST`
- **请求头**: `Session-Id: 会话ID`
- **请求体**:
  ```json
  {
    "name": "连接名称",
    "type": "mysql",
    "host": "主机地址",
    "port": 3306,
    "username": "用户名",
    "password": "密码",
    "database": "数据库名",
    "description": "连接描述"
  }
  ```
- **响应**:
  ```json
  {
    "success": true,
    "data": {
      "id": "新连接ID",
      "message": "数据库连接已保存"
    }
  }
  ```
- **说明**: 保存新的数据库连接配置

### 执行数据库查询
- **路径**: `/api/visualization/query`
- **方法**: `POST`
- **请求头**: `Session-Id: 会话ID`
- **请求体**:
  ```json
  {
    "connection_id": "连接ID",
    "query": "SELECT * FROM table_name",
    "limit": 1000
  }
  ```
- **响应**:
  ```json
  {
    "success": true,
    "data": {
      "columns": ["列名1", "列名2"],
      "rows": [
        {"列名1": "值1", "列名2": "值2"},
        {"列名1": "值3", "列名2": "值4"}
      ],
      "total": 2
    }
  }
  ```
- **说明**: 执行SQL查询并返回结果，limit参数限制返回的最大行数

### 保存可视化配置
- **路径**: `/api/visualization/configs`
- **方法**: `POST`
- **请求头**: `Session-Id: 会话ID`
- **请求体**:
  ```json
  {
    "title": "可视化标题",
    "type": "chart",
    "dataSource": "database",
    "config": {
      "chartType": "line",
      "height": 300,
      "dbType": "mysql",
      "connectionId": "连接ID",
      "query": "SELECT * FROM table_name"
    }
  }
  ```
- **响应**:
  ```json
  {
    "success": true,
    "data": {
      "id": "配置ID",
      "message": "可视化配置已保存"
    }
  }
  ```
- **说明**: 保存用户的可视化配置

### 获取保存的可视化配置
- **路径**: `/api/visualization/configs`
- **方法**: `GET`
- **请求头**: `Session-Id: 会话ID`
- **响应**:
  ```json
  {
    "success": true,
    "data": [
      {
        "id": "配置ID",
        "title": "可视化标题",
        "type": "chart",
        "dataSource": "database",
        "config": {
          "chartType": "line",
          "height": 300,
          "dbType": "mysql",
          "connectionId": "连接ID",
          "query": "SELECT * FROM table_name"
        },
        "created_at": "创建时间",
        "updated_at": "更新时间"
      }
    ]
  }
  ```
- **说明**: 获取用户保存的所有可视化配置
