前端依赖安装
cd frontend
npm install

后端依赖安装
cd backend
pip install -r requirements.txt

前端运行
cd frontend
npm run serve

后端运行
cd backend
python -m app.main


前端和后端的URL配置需要保持一致，默认后端在 http://localhost:5010 运行。
确保 frontend/dist 目录下的静态文件可以被 FastAPI 服务访问到，需要在 app/main.py 中正确配置静态文件路径。